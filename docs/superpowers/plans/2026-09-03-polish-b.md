# Полировка B: план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Кроссфейд кадра проекта при смене языка, обводка ручкой вместо маркера, злеющая от кликов кнопка «me».

**Architecture:** `ProjectShot` становится стопкой из двух слоёв `img`. `PenStroke` заменяет `Marker`: инлайновый `svg` с одним путём, штрих рисуется через `stroke-dashoffset`. `useAnger` держит уровень злости и остывание, `AppHeader` переводит его в цвет и тряску через CSS-переменную `--anger`.

**Tech Stack:** Vue 3.5 `<script setup>` + TypeScript, `@vueuse/core` (`useElementVisibility`), Tailwind 4 с токенами в `src/style.css`, vitest + jsdom + `@vue/test-utils`, pnpm.

Спек: `docs/superpowers/specs/2026-09-03-polish-b-design.md`.

## Global Constraints

- Пакетный менеджер `pnpm`: `pnpm test:run`, `pnpm type-check`, `pnpm lint`, `pnpm build`.
- **Автор параллельно правит `src/pages/ProjectsPage.vue` в рабочем дереве. Этот файл не трогать, не стейджить и не коммитить.** Стейджить только файлы, названные в задаче; никогда `git add -A`. Если `pnpm lint --fix` переформатировал чужой файл, откатить это `git checkout -- <файл>` перед коммитом.
- Никаких `border`; `transition` только по `opacity`, `color`, `transform`, `scale`, `translate`, `stroke-dashoffset`; никакого `transition-all`.
- Под `prefers-reduced-motion: reduce` анимации выключаются, остаётся только цвет.
- Комментарии в коде только там, где без них не понять «почему»; на русском. Автор недавно удалил несколько поясняющих комментариев, так что коротко.
- Токены: `--dur-reveal` (300ms), `--dur-hover`, `--dur-press`, `--ease-standard`, `--primary`, `--danger`, `--fg` уже есть в `src/style.css`.
- Сообщения коммитов на английском в стиле `git log`.

## Карта файлов

| Файл | Ответственность |
|---|---|
| `src/components/project-list/ProjectShot.vue` | Стопка слоёв, кроссфейд |
| `src/components/project-list/ProjectShot.spec.ts` | Тесты кадра, включая кроссфейд |
| `src/components/ui/pen-stroke/PenStroke.vue`, `index.ts`, `PenStroke.spec.ts` | Обводка ручкой |
| `src/components/ui/marker/*` | Удаляется |
| `src/style.css` | `.pen-stroke*` вместо `.marker*`; `.brand-anger`, `@keyframes brand-shake` |
| `src/pages/HomePage.vue` | `PenStroke` вместо `Marker` |
| `src/composables/useAnger.ts`, `useAnger.spec.ts` | Уровень злости и остывание |
| `src/components/AppHeader.vue` | Клик по «me»: `poke`, `--anger`, тряска |

---

### Task 1: Кроссфейд в `ProjectShot`

**Files:**
- Modify: `src/components/project-list/ProjectShot.vue`
- Test: `src/components/project-list/ProjectShot.spec.ts`

**Interfaces:**
- Пропсы не меняются: `{ project: Project; locale: PreviewLocale }`. Корень `div.relative.aspect-video.overflow-hidden.bg-sunk` остаётся (на него опираются `ProjectsPage` и Playwright).

- [ ] **Step 1: Обновить тесты**

Прочитать `ProjectShot.spec.ts`. Тест «смена кадра снова прячет картинку до загрузки» заменить, и добавить тест кроссфейда. Фикстуры оставить как есть (проект с `ru` и `en` кадрами ниже назван `withImage`, подставить настоящее имя):

```ts
import { afterEach, beforeEach, vi } from 'vitest'
// …

  it('при смене кадра старый остаётся, пока новый не загрузился', async () => {
    const wrapper = mount(ProjectShot, { props: { project: withImage, locale: 'ru' } })
    await wrapper.get('img').trigger('load')
    await wrapper.setProps({ locale: 'en' })
    const imgs = wrapper.findAll('img')
    expect(imgs).toHaveLength(2)
    expect(imgs[0]!.attributes('src')).toContain('ru')
    expect(imgs[0]!.classes()).toContain('opacity-100')
    expect(imgs[1]!.attributes('src')).toContain('en')
    expect(imgs[1]!.classes()).toContain('opacity-0')
  })

  describe('после загрузки нового кадра', () => {
    beforeEach(() => vi.useFakeTimers())
    afterEach(() => vi.useRealTimers())

    it('новый проявляется, старый удаляется через --dur-reveal', async () => {
      const wrapper = mount(ProjectShot, { props: { project: withImage, locale: 'ru' } })
      await wrapper.get('img').trigger('load')
      await wrapper.setProps({ locale: 'en' })
      await wrapper.findAll('img')[1]!.trigger('load')
      expect(wrapper.findAll('img')[1]!.classes()).toContain('opacity-100')
      vi.advanceTimersByTime(300)
      await nextTick()
      const imgs = wrapper.findAll('img')
      expect(imgs).toHaveLength(1)
      expect(imgs[0]!.attributes('src')).toContain('en')
    })
  })

  it('тот же кадр на другом языке не создаёт второй слой', async () => {
    const wrapper = mount(ProjectShot, { props: { project: onlyEn, locale: 'ru' } })
    await wrapper.setProps({ locale: 'en' })
    expect(wrapper.findAll('img')).toHaveLength(1)
  })
```

(`onlyEn` — проект только с `en` кадром; `nextTick` импортировать из `vue`.) Тесты первой загрузки и плашки с командой остаются.

- [ ] **Step 2: Запустить, убедиться, что новые падают**

Run: `pnpm vitest run src/components/project-list/ProjectShot.spec.ts`
Expected: FAIL на `toHaveLength(2)`.

- [ ] **Step 3: Реализовать стопку**

`ProjectShot.vue`, `<script setup>`:

```ts
import { computed, ref, watch } from 'vue'
import type { PreviewLocale, Project, ProjectTone } from '@/data/projects'
import { resolvePreviewSrc } from './preview'

const props = defineProps<{
  project: Project
  locale: PreviewLocale
}>()

const src = computed(() => resolvePreviewSrc(props.project.preview, props.locale))

interface Layer {
  src: string
  loaded: boolean
}

const REVEAL_MS = 300
const layers = ref<Layer[]>([])
let sweep: ReturnType<typeof setTimeout> | undefined

// Новый кадр ложится сверху; старый остаётся, пока новый не загрузился.
watch(
  src,
  (next) => {
    if (!next) {
      layers.value = []
      return
    }
    const index = layers.value.findIndex((layer) => layer.src === next)
    if (index >= 0) {
      layers.value.splice(index + 1)
      return
    }
    layers.value.push({ src: next, loaded: false })
  },
  { immediate: true },
)

function markLoaded(layer: Layer) {
  layer.loaded = true
  clearTimeout(sweep)
  sweep = setTimeout(() => {
    const index = layers.value.indexOf(layer)
    if (index > 0) layers.value.splice(0, index)
  }, REVEAL_MS)
}

// Кадр из кэша может быть готов до того, как навесится обработчик load.
function checkComplete(el: unknown, layer: Layer) {
  if (el instanceof HTMLImageElement && el.complete && el.naturalWidth > 0 && !layer.loaded) {
    markLoaded(layer)
  }
}

const TONE_BG: Record<ProjectTone, string> = { /* как было */ }
const TONE_INK: Record<ProjectTone, string> = { /* как было */ }
```

Шаблон:

```vue
<template>
  <div class="relative aspect-video overflow-hidden bg-sunk">
    <template v-if="src">
      <img
        v-for="layer in layers"
        :key="layer.src"
        :ref="(el) => checkComplete(el, layer)"
        :src="layer.src"
        alt=""
        decoding="async"
        fetchpriority="low"
        draggable="false"
        :class="[
          'absolute inset-0 size-full object-cover object-left-top select-none [transition:opacity_var(--dur-reveal)_var(--ease-standard)] motion-reduce:transition-none',
          layer.loaded ? 'opacity-100' : 'opacity-0',
        ]"
        @load="markLoaded(layer)"
      />
    </template>
    <div
      v-else-if="project.preview.kind === 'command'"
      :class="[
        'absolute inset-0 flex items-center justify-center t-code text-[14px]',
        TONE_BG[project.tone],
        TONE_INK[project.tone],
      ]"
    >
      {{ project.preview.command }}
    </div>
  </div>
</template>
```

Убрать прежние `img`/`loaded`/`onMounted`/`useTemplateRef`. `REVEAL_MS` равен токену `--dur-reveal` (300ms); если токен когда-нибудь поменяют, менять оба.

- [ ] **Step 4: Запустить тесты**

Run: `pnpm vitest run src/components/project-list/ProjectShot.spec.ts`
Expected: PASS (в файле было 6 тестов; стало 8: один заменён, два добавлены).

- [ ] **Step 5: Проверить и закоммитить**

Run: `pnpm type-check && pnpm lint && pnpm test:run` (ожидается 38). Если lint тронул `src/pages/ProjectsPage.vue`, откатить его `git checkout -- src/pages/ProjectsPage.vue`.

```bash
git add src/components/project-list/ProjectShot.vue src/components/project-list/ProjectShot.spec.ts
git commit -m "Crossfade project frames when the language changes"
```

---

### Task 2: `PenStroke` вместо `Marker`

**Files:**
- Create: `src/components/ui/pen-stroke/PenStroke.vue`, `src/components/ui/pen-stroke/index.ts`
- Test: `src/components/ui/pen-stroke/PenStroke.spec.ts`
- Delete: `src/components/ui/marker/Marker.vue`, `index.ts`, `Marker.spec.ts`
- Modify: `src/style.css`, `src/pages/HomePage.vue`

- [ ] **Step 1: Падающий тест**

`src/components/ui/pen-stroke/PenStroke.spec.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import { PenStroke } from '.'

describe('PenStroke', () => {
  it('оборачивает текст и рисует один путь', () => {
    const wrapper = mount(PenStroke, { slots: { default: 'дня' } })
    const root = wrapper.get('span.pen-stroke')
    expect(root.text()).toBe('дня')
    expect(root.findAll('svg path')).toHaveLength(1)
    expect(root.get('svg').attributes('aria-hidden')).toBe('true')
  })

  it('без IntersectionObserver штрих показан сразу', () => {
    const wrapper = mount(PenStroke, { slots: { default: 'дня' } })
    expect(wrapper.get('span.pen-stroke').classes()).toContain('pen-stroke-drawn')
  })
})
```

- [ ] **Step 2: Запустить, убедиться, что падает** (`Failed to resolve import "."`).

- [ ] **Step 3: Компонент и индекс**

`PenStroke.vue`:

```vue
<script setup lang="ts">
import { ref, useTemplateRef, watch } from 'vue'
import { useElementVisibility } from '@vueuse/core'

const el = useTemplateRef<HTMLElement>('el')
const canObserve = typeof IntersectionObserver !== 'undefined'
const visible = useElementVisibility(el)
const drawn = ref(!canObserve)

const stop = watch(visible, (isVisible) => {
  if (isVisible) {
    drawn.value = true
    stop()
  }
})
</script>

<template>
  <span ref="el" class="pen-stroke" :class="{ 'pen-stroke-drawn': drawn }">
    <slot />
    <svg class="pen-stroke-ink" viewBox="0 0 100 40" preserveAspectRatio="none" aria-hidden="true">
      <path
        d="M 6 22 C 10 6, 58 2, 90 8 C 104 14, 98 34, 60 37 C 26 39, 0 33, 4 20 C 7 12, 30 9, 52 9"
        pathLength="1"
      />
    </svg>
  </span>
</template>
```

`index.ts`: `export { default as PenStroke } from './PenStroke.vue'`.

- [ ] **Step 4: Стили**

В `src/style.css` внутри `@layer components` удалить блок `.marker` … `.marker-drawn::before` вместе с его `@media (prefers-reduced-motion)` и на его место поставить:

```css
  /* Обводка ручкой: svg чуть больше слова, штрих рисуется по stroke-dashoffset. */
  .pen-stroke {
    position: relative;
    display: inline-block;
    white-space: nowrap;
  }

  .pen-stroke-ink {
    position: absolute;
    left: -0.4em;
    top: -0.35em;
    width: calc(100% + 0.8em);
    height: calc(100% + 0.7em);
    overflow: visible;
    pointer-events: none;
  }

  .pen-stroke-ink path {
    fill: none;
    stroke: var(--primary);
    stroke-width: 1.75;
    vector-effect: non-scaling-stroke;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-dasharray: 1;
    stroke-dashoffset: 1;
  }

  .pen-stroke-drawn .pen-stroke-ink path {
    stroke-dashoffset: 0;
    transition: stroke-dashoffset 0.6s var(--ease-standard) 0.25s;
  }

  @media (prefers-reduced-motion: reduce) {
    .pen-stroke-ink path,
    .pen-stroke-drawn .pen-stroke-ink path {
      stroke-dashoffset: 0;
      transition: none;
    }
  }
```

- [ ] **Step 5: `HomePage.vue` и удаление маркера**

В `HomePage.vue`: `import { Marker } from '@/components/ui/marker'` → `import { PenStroke } from '@/components/ui/pen-stroke'`, в слоте `#day` `<Marker>` → `<PenStroke>`. Удалить папку `src/components/ui/marker` целиком (`git rm -r src/components/ui/marker`).

- [ ] **Step 6: Проверить**

Run: `pnpm vitest run src/components/ui/pen-stroke/PenStroke.spec.ts && pnpm type-check && pnpm lint && pnpm test:run` (ожидается 38: минус 2 теста маркера, плюс 2).

`pnpm dev`: в «Связь» вокруг «дня» синяя петля, рисуется при появлении секции в кадре с небольшой задержкой; на английском вокруг «a day»; в тёмной теме видна; при узком окне слово не переносится.

- [ ] **Step 7: Commit**

```bash
git add src/components/ui/pen-stroke src/style.css src/pages/HomePage.vue
git rm -r -q src/components/ui/marker
git commit -m "Circle the reply time with a pen stroke instead of a marker"
```

---

### Task 3: Злая кнопка «me»

**Files:**
- Create: `src/composables/useAnger.ts`
- Test: `src/composables/useAnger.spec.ts`
- Modify: `src/components/AppHeader.vue`, `src/style.css`

**Interfaces:**
```ts
export const ANGER_STEP = 0.2
export const COOL_INTERVAL_MS = 100
export const COOL_STEP = 0.005
export function useAnger(): { level: Ref<number>; shakeKey: Ref<number>; poke: () => void }
```

- [ ] **Step 1: Падающие тесты**

`src/composables/useAnger.spec.ts`:

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { useAnger } from './useAnger'

describe('useAnger', () => {
  beforeEach(() => vi.useFakeTimers())
  afterEach(() => vi.useRealTimers())

  it('один клик поднимает злость на 0.2', () => {
    const { level, poke } = useAnger()
    poke()
    expect(level.value).toBeCloseTo(0.2)
  })

  it('не злится выше единицы', () => {
    const { level, poke } = useAnger()
    for (let i = 0; i < 7; i++) poke()
    expect(level.value).toBe(1)
  })

  it('после одного клика остывает за четыре секунды', () => {
    const { level, poke } = useAnger()
    poke()
    vi.advanceTimersByTime(2000)
    expect(level.value).toBeGreaterThan(0)
    vi.advanceTimersByTime(2100)
    expect(level.value).toBe(0)
  })

  it('каждый клик перезапускает тряску', () => {
    const { shakeKey, poke } = useAnger()
    poke()
    poke()
    expect(shakeKey.value).toBe(2)
  })
})
```

- [ ] **Step 2: Запустить, убедиться, что падает.**

- [ ] **Step 3: Композабл**

`src/composables/useAnger.ts`:

```ts
import { getCurrentScope, onScopeDispose, ref } from 'vue'

export const ANGER_STEP = 0.2
export const COOL_INTERVAL_MS = 100
export const COOL_STEP = 0.005

// Злость растёт от кликов и остывает сама: один клик отпускает примерно за 4 с.
export function useAnger() {
  const level = ref(0)
  const shakeKey = ref(0)
  let timer: ReturnType<typeof setInterval> | undefined

  const stop = () => {
    if (timer !== undefined) {
      clearInterval(timer)
      timer = undefined
    }
  }

  const cool = () => {
    level.value = Math.max(0, Number((level.value - COOL_STEP).toFixed(3)))
    if (level.value === 0) stop()
  }

  const poke = () => {
    level.value = Math.min(1, Number((level.value + ANGER_STEP).toFixed(3)))
    shakeKey.value += 1
    if (timer === undefined) timer = setInterval(cool, COOL_INTERVAL_MS)
  }

  if (getCurrentScope()) onScopeDispose(stop)

  return { level, shakeKey, poke }
}
```

- [ ] **Step 4: Запустить тесты** — PASS, 4.

- [ ] **Step 5: `AppHeader.vue`**

В `<script setup>` добавить:

```ts
import { ref, watch } from 'vue' // ref уже импортирован, добавить watch и nextTick при необходимости
import { useAnger } from '@/composables/useAnger'

const { level: anger, shakeKey, poke } = useAnger()
const shaking = ref(false)

// Повторный клик во время тряски: снять класс и вернуть на следующем кадре, чтобы анимация началась заново.
watch(shakeKey, () => {
  shaking.value = false
  requestAnimationFrame(() => {
    shaking.value = true
  })
})

const onBrandClick = () => {
  cue('tick')
  poke()
}
```

`RouterLink` бренда:

```vue
    <RouterLink
      :to="routeLocation.home()"
      class="brand-anger press-scale t-subheading shrink-0 rounded-2 no-underline"
      :class="{ 'is-shaking': shaking }"
      :style="{ '--anger': anger }"
      @click="onBrandClick"
      @animationend="shaking = false"
    >
```

- [ ] **Step 6: Стили**

В `src/style.css`, `@layer components`, после `.press-scale`-блока:

```css
  /* Злая «me»: цвет и амплитуда тряски растут с --anger от 0 до 1. */
  a.brand-anger {
    color: color-mix(in oklch, var(--fg), var(--danger) calc(var(--anger, 0) * 100%));
    transition:
      color var(--dur-hover) ease,
      transform var(--dur-press) var(--ease-standard);
  }

  .brand-anger.is-shaking {
    animation: brand-shake 0.35s var(--ease-standard);
  }

  @keyframes brand-shake {
    0%,
    100% {
      transform: translateX(0) rotate(0);
    }
    20%,
    60% {
      transform: translateX(calc(-2px - 6px * var(--anger, 0))) rotate(calc(-1deg - 3deg * var(--anger, 0)));
    }
    40%,
    80% {
      transform: translateX(calc(2px + 6px * var(--anger, 0))) rotate(calc(1deg + 3deg * var(--anger, 0)));
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .brand-anger.is-shaking {
      animation: none;
    }
  }
```

`.t-subheading` задаёт `color: var(--fg)` и объявлен в том же слое позже, поэтому цвет вешается на селектор `a.brand-anger` (специфичность выше), а не на `.brand-anger`.

- [ ] **Step 7: Проверить**

Run: `pnpm type-check && pnpm lint && pnpm test:run` (ожидается 42).

`pnpm dev`: пять быстрых кликов по «me» — надпись краснеет и трясётся всё сильнее; через несколько секунд без кликов возвращается к обычному цвету; в reduced motion только цвет; клик со страницы `/projects` по-прежнему ведёт на главную.

- [ ] **Step 8: Commit**

```bash
git add src/composables/useAnger.ts src/composables/useAnger.spec.ts src/components/AppHeader.vue src/style.css
git commit -m "Make the brand link angrier with every click"
```

---

### Task 4: Финальный прогон

- [ ] `pnpm lint && pnpm type-check && pnpm test:run && pnpm build` — всё зелёное; `git status --short` показывает только авторский `src/pages/ProjectsPage.vue` (его не трогать).
- [ ] Playwright-проверка спека делает контроллер.
